spring:
  application:
    name: Authorization Service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: password
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: off
server:
  port: 9000

token:
  secret: krOTDgSoVxe2xRP6QrGzVCuldrCRhnuB
  access-token-ttl: 60
  refresh-token-ttl: 120
  authorization-code-ttl: 3600


